# 🏗️ Diagrama de Arquitectura - AlertaClimática

## Arquitectura del Sistema

```
┌─────────────────────────────────────────────────────────────────┐
│                         FRONTEND (Cliente)                       │
│                                                                  │
│  ┌──────────────────┐  ┌──────────────────┐  ┌───────────────┐ │
│  │   Página Pública │  │  Login Page      │  │ Panel Admin   │ │
│  │   (HTML/CSS/JS)  │  │  (admin/login)   │  │ (admin/panel) │ │
│  │                  │  │                  │  │               │ │
│  │  - index.html    │  │  - Validación    │  │ - Dashboard   │ │
│  │  - noticias.html │  │  - Auth JWT      │  │ - CRUD News   │ │
│  │  - mapa.html     │  │  - Seguridad     │  │ - Stats       │ │
│  │  - foro.html     │  │                  │  │               │ │
│  └────────┬─────────┘  └────────┬─────────┘  └───────┬───────┘ │
│           │                     │                    │          │
└───────────┼─────────────────────┼────────────────────┼──────────┘
            │                     │                    │
            │                     ▼                    ▼
            │           ┌─────────────────────────────────┐
            │           │    Autenticación JWT             │
            │           │    - Token Bearer                │
            │           │    - Headers Authorization       │
            │           │    - LocalStorage                │
            │           └─────────────────────────────────┘
            │                     │                    │
            ▼                     ▼                    ▼
┌─────────────────────────────────────────────────────────────────┐
│                         API REST (Backend)                       │
│                      Node.js + Express                           │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │                   MIDDLEWARES DE SEGURIDAD                  │ │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────┐  │ │
│  │  │  Helmet  │ │   CORS   │ │   Rate   │ │  Sanitizer   │  │ │
│  │  │ Headers  │ │ Security │ │ Limiting │ │   NoSQL      │  │ │
│  │  └──────────┘ └──────────┘ └──────────┘ └──────────────┘  │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │                          RUTAS                              │ │
│  │                                                             │ │
│  │  📍 /api/auth/                                             │ │
│  │     ├─ POST   /login          (público)                   │ │
│  │     ├─ POST   /registro       (público)                   │ │
│  │     ├─ GET    /perfil         (protegido)                 │ │
│  │     └─ GET    /verificar      (protegido)                 │ │
│  │                                                             │ │
│  │  📍 /api/noticias/                                         │ │
│  │     ├─ GET    /                (público)                   │ │
│  │     ├─ GET    /:id             (público)                   │ │
│  │     ├─ POST   /                (protegido)                 │ │
│  │     ├─ PUT    /:id             (protegido)                 │ │
│  │     ├─ DELETE /:id             (protegido - admin)         │ │
│  │     └─ GET    /stats/general   (protegido)                 │ │
│  │                                                             │ │
│  │  📍 /api/health                (público)                   │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │                      CONTROLADORES                          │ │
│  │  ┌─────────────────┐          ┌──────────────────┐        │ │
│  │  │ authController  │          │ noticiaController│        │ │
│  │  │  - login        │          │  - obtener       │        │ │
│  │  │  - registro     │          │  - crear         │        │ │
│  │  │  - perfil       │          │  - actualizar    │        │ │
│  │  │  - verificar    │          │  - eliminar      │        │ │
│  │  └─────────────────┘          └──────────────────┘        │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │                      MIDDLEWARES                            │ │
│  │  ┌──────────────┐  ┌──────────────┐  ┌─────────────────┐  │ │
│  │  │    Auth      │  │  Validator   │  │    Security     │  │ │
│  │  │ - proteger   │  │ - validar    │  │ - rate limit    │  │ │
│  │  │ - autorizar  │  │ - sanitizar  │  │ - sanitize      │  │ │
│  │  └──────────────┘  └──────────────┘  └─────────────────┘  │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │                        MODELOS                              │ │
│  │  ┌─────────────────┐          ┌──────────────────┐        │ │
│  │  │     User        │          │     Noticia      │        │ │
│  │  │  - nombre       │          │  - titulo        │        │ │
│  │  │  - email        │          │  - contenido     │        │ │
│  │  │  - password     │          │  - categoria     │        │ │
│  │  │  - rol          │          │  - gravedad      │        │ │
│  │  │  - activo       │          │  - autor         │        │ │
│  │  └─────────────────┘          └──────────────────┘        │ │
│  └────────────────────────────────────────────────────────────┘ │
│                              │                                   │
└──────────────────────────────┼───────────────────────────────────┘
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│                     BASE DE DATOS (MongoDB)                      │
│                                                                  │
│  ┌──────────────────┐              ┌──────────────────┐        │
│  │  Colección:      │              │  Colección:      │        │
│  │     users        │              │    noticias      │        │
│  │                  │              │                  │        │
│  │  - _id           │              │  - _id           │        │
│  │  - nombre        │              │  - titulo        │        │
│  │  - email         │              │  - contenido     │        │
│  │  - password      │◄─────────────┤  - autor (ref)   │        │
│  │  - rol           │              │  - categoria     │        │
│  │  - activo        │              │  - gravedad      │        │
│  │  - timestamps    │              │  - timestamps    │        │
│  └──────────────────┘              └──────────────────┘        │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

## Flujo de Autenticación

```
┌──────────┐                ┌──────────┐                ┌──────────┐
│  Cliente │                │  Backend │                │ MongoDB  │
└────┬─────┘                └────┬─────┘                └────┬─────┘
     │                           │                           │
     │ 1. POST /api/auth/login   │                           │
     │  { email, password }      │                           │
     ├──────────────────────────►│                           │
     │                           │                           │
     │                           │ 2. Buscar usuario         │
     │                           ├──────────────────────────►│
     │                           │                           │
     │                           │ 3. Usuario encontrado     │
     │                           │◄──────────────────────────┤
     │                           │                           │
     │                           │ 4. Comparar password      │
     │                           │    (bcrypt)               │
     │                           │                           │
     │                           │ 5. Generar JWT Token      │
     │                           │                           │
     │ 6. Token + Usuario        │                           │
     │◄──────────────────────────┤                           │
     │                           │                           │
     │ 7. Guardar en LocalStorage│                           │
     │                           │                           │
     │                           │                           │
     │ 8. Peticiones protegidas  │                           │
     │    Authorization: Bearer  │                           │
     │    {token}                │                           │
     ├──────────────────────────►│                           │
     │                           │                           │
     │                           │ 9. Verificar JWT          │
     │                           │                           │
     │                           │ 10. Validar usuario activo│
     │                           ├──────────────────────────►│
     │                           │                           │
     │                           │ 11. Usuario válido        │
     │                           │◄──────────────────────────┤
     │                           │                           │
     │ 12. Respuesta autorizada  │                           │
     │◄──────────────────────────┤                           │
     │                           │                           │
```

## Capas de Seguridad Implementadas

### 1. **Capa de Transporte**
- ✅ HTTPS (en producción)
- ✅ Headers seguros (Helmet)
- ✅ CORS configurado

### 2. **Capa de Autenticación**
- ✅ JWT (JSON Web Tokens)
- ✅ Bcrypt para hash de contraseñas
- ✅ Tokens con expiración

### 3. **Capa de Autorización**
- ✅ Roles de usuario (admin, editor)
- ✅ Middlewares de protección de rutas
- ✅ Validación de permisos

### 4. **Capa de Validación**
- ✅ express-validator
- ✅ Sanitización de entradas
- ✅ Prevención de inyección NoSQL

### 5. **Capa de Rate Limiting**
- ✅ Rate limiter en login (5 intentos/15min)
- ✅ Rate limiter general (100 req/15min)

### 6. **Capa de Datos**
- ✅ Variables de entorno (.env)
- ✅ Passwords nunca en texto plano
- ✅ Validaciones en modelos

## Tecnologías Utilizadas

### Frontend
- HTML5
- CSS3 (Variables CSS, Grid, Flexbox)
- JavaScript (ES6+)
- Bootstrap (parcial)
- Fetch API

### Backend
- Node.js v18+
- Express.js
- MongoDB + Mongoose
- JWT (jsonwebtoken)
- Bcryptjs
- Helmet
- CORS
- Express Validator
- Morgan (logger)

### Herramientas
- Git (versionamiento)
- npm (gestor de paquetes)
- dotenv (variables de entorno)
- nodemon (desarrollo)

## Metodología

### Scrum
- **Scrum Master**: Elena Natalia Diosdado Arellano
- **Product Owner**: José Armando Valenzuela Olivares

### Arquitectura
- **Patrón**: MVC (Modelo-Vista-Controlador)
- **Estilo**: API REST
- **Autenticación**: JWT Bearer Token

## Versionamiento

- **Versión Actual**: v1.0.0
- **Estrategia**: Semantic Versioning (SemVer)
- **Git Flow**: main, develop, feature branches

## Despliegue (Futuro)

### Backend
- Railway.app
- Heroku
- AWS EC2
- DigitalOcean

### Frontend
- Netlify
- Vercel
- GitHub Pages

### Base de Datos
- MongoDB Atlas (Cloud)

---

**Desarrollado con ❤️ por el equipo de AlertaClimática**

